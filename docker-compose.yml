version: "3.9"

services:
  vue:
    build: .
    container_name: ecommerce_vue
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host
